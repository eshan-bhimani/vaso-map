/**
 * PostCSS configuration.
 *
 * Educational note: PostCSS is a tool for transforming CSS.
 * Tailwind CSS requires these plugins:
 * - tailwindcss: Processes Tailwind directives
 * - autoprefixer: Adds vendor prefixes for browser compatibility
 */

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
